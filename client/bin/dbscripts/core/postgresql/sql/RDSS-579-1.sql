delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Accrual Date'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Agency'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Announce Date'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Auction Date'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Bankruptcy File Date'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Convertible Flag'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Day Count Code'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Minimum Denomination'
;
delete from reutersdss_mapping where intf_type='RDSS' and typename='columns.Terms.and.Conditions.Mandatory' and intf_value='Face Amount'
;